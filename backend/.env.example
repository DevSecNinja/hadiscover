# GitHub API Configuration
GITHUB_TOKEN=your_github_personal_access_token_here

# Database Configuration
DATABASE_URL=sqlite:///./ha_discover.db

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Root Path Configuration (optional)
# Set this when deploying behind a reverse proxy or on cloud platforms
# that require a specific base path. Leave empty for default behavior.
# Example: ROOT_PATH=/api/v1 if your cloud platform handles routing
# and you want the app accessible at the root URL
ROOT_PATH=
